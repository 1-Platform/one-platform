import { TimelineScoreFormaterPipe } from './timeline-score-formater.pipe';

describe('TimelineScoreFormaterPipe', () => {
  it('create an instance', () => {
    const pipe = new TimelineScoreFormaterPipe();
    expect(pipe).toBeTruthy();
  });
});
